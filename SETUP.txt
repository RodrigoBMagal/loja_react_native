Este arquivo foi substituido pelas instrucoes atualizadas no README.md
(TypeScript, Prisma, Docker Compose e CI).

Resumo rapido:

  cp .env.example .env
  docker compose up --build
  docker compose exec backend npm run seed

Depois: npm install && npm start (na raiz do projeto) para o app React Native.

Veja o README.md para o passo a passo completo, inclusive sem Docker.
